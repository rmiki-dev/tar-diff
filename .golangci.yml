version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - misspell
    - gocritic
    - revive
    - ineffassign
    - unused
    - gocyclo

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  